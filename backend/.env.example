# Relay Backend Environment Configuration Example

# Server Port (Default: 8080)
PORT=8080

# MongoDB Connection URI (Local MongoDB or MongoDB Atlas)
MONGODB_URI=mongodb://localhost:27017/streaksaver

# JWT Secret Key for authenticating Relay API tokens
JWT_SECRET=relay_secret_key_protection_system_default_key_2026_super_secure
