spring.application.name=relay-backend
server.port=${PORT:8080}

# MongoDB Database Configuration
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/streaksaver}

# JWT Authentication Secret Key
jwt.secret=${JWT_SECRET:relay_secret_key_protection_system_default_key_2026_super_secure}
